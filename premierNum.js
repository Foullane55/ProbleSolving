var prime =  (n) => {
  let numbers = [2,3,5]
  for (let i = 0; i < numbers.length; i++) {
    if (n == numbers[i]) {
      return true
    }
    if (n%numbers[i] == 0) {
      return fals
    }
  }
  }
